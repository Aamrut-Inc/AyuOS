{
  "name": "ayuos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "bun run load/migrate.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
