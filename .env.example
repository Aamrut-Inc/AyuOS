# Local Postgres connection used by load/.
# Matches the ayuos-db service in docker-compose.yml (docker compose up -d).
DATABASE_URL=postgres://ayuos:ayuos@localhost:5433/ayuos

# Open Wearables (services/wearables) instance to read synced wearable data from.
# The local default below matches the wearables-app service in docker-compose.yml.
# OPEN_WEARABLES_API_KEY is not a static secret: it's created by running
# services/wearables/scripts/create-dev-api-key.sh after `docker compose up -d`.
OPEN_WEARABLES_API_BASE_URL=http://localhost:8000
OPEN_WEARABLES_API_KEY=
OPEN_WEARABLES_USER_ID=

# EHR/FHIR (Epic sandbox by default). Ask a teammate for real values, or
# register your own free sandbox app at https://fhir.epic.com (no real
# patient data involved — it's Epic's open non-production sandbox).
FHIR_PROVIDER=epic_sandbox
FHIR_BASE_URL=https://fhir.epic.com/interconnect-fhir-oauth/api/FHIR/R4
FHIR_CLIENT_ID=
FHIR_REDIRECT_URI=http://127.0.0.1:8765/callback
FHIR_SCOPES=launch/patient openid fhirUser patient/Patient.rs patient/Observation.rs patient/Immunization.rs patient/DiagnosticReport.rs patient/DocumentReference.rs patient/Encounter.rs patient/Procedure.rs
FHIR_OBSERVATION_CATEGORY=survey
FHIR_RESOURCE_TYPES=Patient,Observation,Immunization,DiagnosticReport,DocumentReference,Encounter,Procedure
